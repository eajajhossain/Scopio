"""Autonomous inbound email: poll the tenant's inbox (IMAP), route each customer
reply to the AI sales agent, and send the agent's response back over SMTP — so the
agent holds the whole conversation with every contacted business, hands-free.
"""
